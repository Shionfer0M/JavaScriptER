// Ciclo Do While
// Ejecuta todo el código pasa 1 vez y abajo esta la condición y si falla ya no se ejecuta pero ya guardo el primero valor la primera vez
// Setup
var myArray = [];
var i = 10;

// Only change code below this line
do{
  myArray.push(i);
  i++;
} while (i < 5)