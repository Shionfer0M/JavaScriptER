// Cambiando la sentencia if/else por una comparación exacta
function isLess(a, b) {
  // Cambia solo el código debajo de esta línea
  return a < b;
  /* Cambiamos este código
  if (a < b) {
    return true;
  } else {
    return false;
  } */
  // Cambia solo el código encima de esta línea
}

isLess(10, 15);