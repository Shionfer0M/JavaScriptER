// Guardamos el valor que retorna una función a una variable
// Configuración
var processed = 0;

function processArg(num) {
  return (num + 3) / 5;
}

processed = processArg(7)

// Cambia solo el código debajo de esta línea