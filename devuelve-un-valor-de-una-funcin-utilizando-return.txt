// Puedes utilizar una declaración de devolución ( return) para enviar un valor fuera de una función.
// Crea una función timesFiveque acepte un argumento, lo multiplique por 5y devuelva el nuevo valor.
function timesFive(a){
  return a*5;
}

timesFive(2);
