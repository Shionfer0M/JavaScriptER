/* JavaScript tiene una Math.random()función que genera un número decimal aleatorio entre 0(inclusivo) y 1(exclusivo). Por Math.random()lo tanto, puede devolver un 0pero nunca devolver un 1. */
function randomFraction() {

  // Only change code below this line

  return Math.random();

  // Only change code above this line
}