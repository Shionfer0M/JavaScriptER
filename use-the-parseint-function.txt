function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");

/* La parseInt()función analiza una cadena y devuelve un número entero. He aquí un ejemplo:

var a = parseInt("007");
La función anterior convierte la cadena 007en un número entero 7. Si el primer carácter de la cadena no se puede convertir en un número, regresa NaN.

*/