// Configuración
var myArray = [["John", 23], ["dog", 3]];
// Se elimina el primer elemento del arreglo
myArray.shift();
// Cambia solo el código debajo de esta línea
// .unshift()funciona exactamente como .push(), pero en lugar de agregar el elemento al final del arreglo, unshift()agrega el elemento al principio del arreglo.

// Se agrega un eleento a la primer posición

myArray.unshift(['Paul',35]);
