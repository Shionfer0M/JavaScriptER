// Utulizando y && si las 2 condiciones son verdaderas
// También se puede ocupar if anidados
function testLogicalAnd(val) {
  // Cambia solo el código debajo de esta línea

  if (val <= 50 && val >= 25) {
    return "Yes";
  }
  // Cambia solo el código encima de esta línea
  return "No";
}

testLogicalAnd(10);